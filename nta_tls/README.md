# Reference — teammate NTA / TLS challenge

These files were created by other team members. **Do not reuse them** for the F26 Log Lab.

| File | Purpose |
|------|---------|
| `NTA_challenge.pcap` | Packet capture with TLS; find suspicious site, decrypt, Follow TLS stream |
| `keys.log` | SSLKEYLOGFILE / (Pre)-Master-Secret log for Wireshark TLS decrypt |

The **F26 Log Lab** (`../F26_log_lab/`) covers related analyst skills with **original** cleartext HTTP + log artifacts instead.
